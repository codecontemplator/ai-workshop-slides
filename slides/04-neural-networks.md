## Neural Networks in One Sentence

> Layers of math that learn patterns from data by adjusting millions (or billions) of numerical weights.

### The intuition:

- **Input** → text converted to numbers
- **Hidden layers** → transform and find patterns
- **Output** → probability distribution over possible next tokens

Each "weight" is a knob. Training = turning billions of knobs until the output looks right.
